apply plugin: "java"
apply plugin: "maven-publish"

dependencies {
	compile gradleApi()
}

/*
publishing {
	publications {
		toolingJar(MavenPublication) {
			artifactId = "com.liferay.blade.gradle.tooling"

			from components.java
		}
	}
}
*/

version = "1.2.3-SNAPSHOT"